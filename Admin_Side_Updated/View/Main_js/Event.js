const create_Event = () =>{    let img;    document.addEventListener('click',(event) =>{        const upload_img = document.getElementById('upload_Event');        upload_img.addEventListener('change',(e) =>{            img = e.target.files[0]        })        if(event.target.id === 'create_Event'){            const Message = document.getElementById('Message_Event')            const Title = document.getElementById('Title_Event')            if(!Message.value || !Title.value || !img){                alert("inputs info")            }else {                const formData = new  FormData()                formData.append('Title',Title.value)                formData.append('img',img)                formData.append('Message',Message.value)                //ajax re                $.ajax({                    url: '../Model/Add_Event.php',                    type: 'post',                    data: formData,                    processData: false,                    contentType: false,                    success: function (data,status){                        $('#modal_Event').modal('hide');                        location.reload()                    }                })            }        }    })}create_Event()const viewUpdateEvent = () =>{    document.addEventListener('click',(event) =>{        if(event.target.id === 'update_event'){            //ajax            $.ajax({                url: '../Model/ViewUpdateEvent.php',                type: 'post',                data:{                    id: event.target.value                },                success: function (data,status){                    const modal_container = document.getElementById('modal_container')                    modal_container.innerHTML = data                    $('#Update_Event').modal('show');                }            })        }    })}viewUpdateEvent()const SaveUpdated_Event = () => {    let SaveImg;    document.addEventListener('click', (event) => {        const upload_img = document.getElementById('upload_Event_save');        if (upload_img !== null) {            upload_img.addEventListener('change', (e) => {                SaveImg = e.target.files[0];            });        }        if (event.target.id === 'Save_Event') {            const Message = document.getElementById('Message_save');            const Title = document.getElementById('Save_title');            const formData = new FormData();            formData.append('Title', Title.value);            formData.append('SaveImg', SaveImg);            formData.append('Message', Message.value);            formData.append('id', event.target.value);            // AJAX request            $.ajax({                url: '../Model/SaveEvent.php',                type: 'POST',                data: formData,                processData: false,                contentType: false,                success: function (data, status) {                    console.log('save');                },                error: function (xhr, status, error) {                    console.error('Error:', error);                }            });        }    });};SaveUpdated_Event();const Delete_event = () =>{    document.addEventListener('click',(event) =>{        if(event.target.id  === 'DEL_event'){            const Confirm = document.getElementById('Confirm');            $('#Confirm_del').modal('show');              console.log(event.target.value)            Confirm.addEventListener('click',() =>{                // AJAX request                $.ajax({                    url: '../Model/Delete_event.php',                    type: 'POST',                    data: {                        id: event.target.value                    },                    success: function (data, status) {1                        $('#Confirm_del').modal('hide');                        location.reload()                    }                });            })        }    })}Delete_event()const View_eve = () =>{   document.addEventListener('click',(event) =>{       if(event.target.id === 'view_event'){           // AJAX request           $.ajax({               url: '../Model/View_event.php',               type: 'POST',               data: {                   id: event.target.value               },               success: function (data, status) {                   const modal_container = document.getElementById('modal_container')                   modal_container.innerHTML = data                   $('#View_Event_modal').modal('show');               }           });       }   })}View_eve()